# Core dependencies (Phase 1 pinned versions)
litellm[proxy]==1.70.0
# prisma is required by LiteLLM proxy's DB layer when DATABASE_URL is set
# (not included in the [proxy] extra of 1.70.0)
prisma==0.13.1
# fastapi/uvicorn pins must satisfy litellm[proxy] 1.70.0 constraints
# (fastapi >=0.115.5,<0.116; uvicorn >=0.29,<0.30)
fastapi==0.115.6
uvicorn[standard]==0.29.0
sqlalchemy==2.0.35
alembic==1.13.2
pydantic==2.9.0
redis==5.0.0
psycopg2-binary==2.9.10
httpx==0.27.0
python-dotenv==1.0.1
pyyaml==6.0.1
supervisor==4.2.5
apscheduler==3.10.4
jinja2==3.1.4
bcrypt==5.0.0
itsdangerous==2.2.0
python-multipart==0.0.18

# Development dependencies
pytest==8.3.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0
faker==25.0.0
pytest-mock==3.14.0
black==24.8.0
ruff==0.6.0
mypy==1.11.0
